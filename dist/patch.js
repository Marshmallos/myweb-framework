export function patch(newNode, oldNode) {
    if (newNode.tag !== oldNode.tag) {
    }
    for (const key in newNode.attrs) {
        if (oldNode.attrs[key] === undefined) {
        }
        else if (newNode.attrs[key] !== oldNode.attrs[key]) {
        }
        else {
        }
    }
}
//# sourceMappingURL=patch.js.map